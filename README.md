# Simulation-project
The Student Record Management System (SRMS) is a C-based console application designed to manage student records efficiently using file handling. It provides secure login access and supports three different user roles: Admin, Staff, and Guest, each with specific permissions.
